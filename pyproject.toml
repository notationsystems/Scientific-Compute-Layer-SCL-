[project]
name = "scientific-compute-layer"
version = "0.1.0"
description = "SCL: a reusable native C/C++/CUDA Scientific Compute Layer, usable standalone or integrated into the Scientific Transformer Engine (STE) via scl.ste_adapter."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
